#include <stdio.h>

int main()
{
    int s;
    scanf("%d", &s);
    int h = s/(60*60);
    int m = ((s-(h*60*60))/60);
    int S = ((s-(h*60*60))-(m*60));
    printf("%d hour %d minute %d second", h,m,S);
    return 0;
}
